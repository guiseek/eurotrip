{
  "name": "eurotrip",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tree": "tree -fDJ public/photos/ --noreport --sort mtime --device --info --inodes -o public/photos.json"
  },
  "devDependencies": {
    "sass": "^1.56.2",
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  }
}
